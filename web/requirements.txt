# Dependencies for the hosted web app (web/streamlit_app.py -> app.py).
# Only the API backend is used when hosted, so no torch / transformers.
# Keep in step with requirements-api.txt.
requests==2.32.5
Pillow==12.1.1
openpyxl==3.1.5
streamlit==1.62.0
pandas==3.0.5
